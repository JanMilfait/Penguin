apps:
  - name: compress-video-job
    script: artisan
    exec_mode: fork
    interpreter: php
    instances: 1
    args:
      - queue:work
      - --sleep=5
      - --rest=1
      - --queue=default
